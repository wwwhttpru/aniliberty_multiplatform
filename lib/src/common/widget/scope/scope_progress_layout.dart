import 'package:aniliberty_multiplatform/src/core/core.dart';
import 'package:flutter/material.dart';

class ScopeProgressLayout extends StatelessWidget {
  const ScopeProgressLayout({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: ProgressLayout());
}
